<!--
SPDX-FileCopyrightText: 2026 MAINLINE contributors
SPDX-License-Identifier: Apache-2.0
-->

# `REFUSAL_DEPTH.md` — the unwelding matrix

**Generated by `unweld/test_unweld.py`. Do not edit.** Every number below was measured by taking one mechanism away, re-running the identical illegal history in a fresh tenancy, and putting the mechanism back.

* profile · `trappoint-ref` · schema `trappoint_ref`
* spec · `1.0.0-rc.1`
* cluster · `CockroachDB CCL v26.2.5`
* histories measured · 27

> **Provenance.** measured on a disposable single-node CockroachDB with the reference vertical's rendered SQL applied. Six relations the reference tree NAMES but does not CREATE -- `trappoint_ref.event`, `.clause`, `.site`, `.event_severity_revision`, `.ledger_intake` and `trappoint_ref_meas.recall_policy` -- were supplied as minimal stand-ins so the tree would apply at all. That gap is a finding about the reference binding, reported upstream, and it is recorded here because a matrix measured on a patched schema is not a matrix measured on the shipped one.

## What this run found

**9 of 9 gated merge-gate histories measured at depth 1, and 15 mechanism(s) opened a gate when removed alone.** The architecture's sentence — *delete the `RAISE` and the write still fails twice over* — does not hold for those histories as the schema currently stands, and this file is where that is said rather than assumed.

The reason is structural rather than accidental, and it is worth stating precisely because it looks like a bug and is not one. `fn_permit_merge_gate` **deliberately declines to decide** when the projected counter agrees with the re-derivation: the refusal then belongs to `gate_closed_when_issued`, whose name is the exhibit, and raising a `P0001` over the top of it would trade a named constraint for an unnamed one (`spec/errors.md` §3.3, corollary). The consequence is that on the ordinary path exactly one mechanism refuses, and on the drift path exactly one other does. Each is correct alone; neither is a second weld for the other.

That is a decision for the kernel lead, and the pre-committed response to a depth of one is on the record: *cut the mechanism, do not ship it.* The measurement is here so the choice is made with a number in front of it.

## What `depth` means here

`depth` is the number of **distinct mechanisms observed to refuse the same history** across the baseline and every single-mechanism removal. It is not the number declared: a mechanism that is in the matrix and never observed is reported as unobserved, because a defence nobody has seen fire is a defence nobody has seen work.

**At runtime the deterministic `RAISE` fires first** (adversarial finding `S4`), and no case in `cases/` asserts otherwise. The redundancy below is a property of the schema, observable only by unwelding it, and this file is the only place the claim is made.

## The matrix

| history | depth | baseline exhibit | surviving mechanisms | unwelded by |
|---|---|---|---|---|
| `CF-01` ⚠ | 1 | `23502` `` | `` | — |
| `CF-02` ⚠ | 1 | `P0001` `mainline.fn_permit_merge_gate` | `mainline.fn_permit_merge_gate` | **`permit@permit_merge_gate`** |
| `CF-03` ⚠ | 1 | `P0001` `mainline.fn_permit_merge_gate` | `mainline.fn_permit_merge_gate` | **`permit@permit_merge_gate`** |
| `CF-04` ⚠ | 1 | `23514` `merge_evidence` | `merge_evidence` | **`permit.merge_evidence`** |
| `CF-05` ⚠ | 1 | `23514` `reading_floor_when_issued` | `reading_floor_when_issued` | **`permit.reading_floor_when_issued`** |
| `CF-06` ⚠ | 1 | `P0001` `mainline.fn_permit_merge_gate` | `mainline.fn_permit_merge_gate` | **`permit@permit_merge_gate`** |
| `CF-07` | 1 | `23503` `fk_clearance` | `fk_clearance` | **`disposition.fk_clearance`** |
| `CF-08` | 1 | `P0001` `mainline.fn_refuse_mutation` | `mainline.fn_refuse_mutation` | **`clause_blame_closure@append_only`** |
| `CF-09` | 1 | `23505` `merge_record_pkey` | `merge_record_pkey` | — |
| `CF-10` | 1 | `P0001` `mainline.fn_check_materialised` | `mainline.fn_check_materialised` | — |
| `CF-12` | 2 | `23505` `one_live_disposition` | `ctr_nonneg`, `one_live_disposition` | — |
| `CF-13` | 1 | `23503` `legal_edge` | `legal_edge` | **`permit_event.legal_edge`** |
| `CF-14` | 1 | `23505` `linear` | `linear` | **`permit_event.linear`** |
| `CF-15` | 1 | `23505` `cr_linear` | `cr_linear` | **`cr_event.cr_linear`** |
| `CF-16` | 1 | `P0001` `mainline.fn_permit_event_chain` | `mainline.fn_permit_event_chain` | **`permit_event@permit_event_chain`** |
| `CF-17` | 1 | `P0001` `mainline.fn_permit_event_chain` | `mainline.fn_permit_event_chain` | **`permit_event@permit_event_chain`** |
| `CF-18` | 1 | `23503` `fk_exposure` | `fk_exposure` | **`disposition.fk_exposure`** |
| `CF-23` | 1 | `23503` `fk_clearance` | `fk_clearance` | **`disposition.fk_clearance`** |
| `CF-31` ⚠ | 1 | `23514` `cr_gate_closed_when_merged` | `cr_gate_closed_when_merged` | **`change_request.cr_gate_closed_when_merged`** |
| `CF-38` | 1 | `P0001` `mainline.fn_disposition_retract_only` | `mainline.fn_disposition_retract_only` | **`disposition@disposition_retract_only`** |
| `CF-39` | 1 | `P0001` `mainline.fn_refuse_mutation` | `mainline.fn_refuse_mutation` | **`permit_event@append_only`** |
| `CF-40` | 1 | `23503` `epoch_pin_permit` | `epoch_pin_permit` | **`merge_record.epoch_pin_permit`**, **`disposition@disposition_retract_only`** |
| `CF-42` | 2 | `P0001` `mainline.fn_check_project` | `fk_check_version`, `mainline.fn_check_project` | — |
| `CF-44` | 1 | `23505` `merge_record_pkey` | `merge_record_pkey` | — |
| `CF-45` ⚠ | 1 | `23514` `gate_closed_when_issued` | `gate_closed_when_issued` | **`permit.gate_closed_when_issued`** |
| `CF-70` ⚠ | 1 | `23514` `gate_closed_when_issued` | `gate_closed_when_issued` | **`permit.gate_closed_when_issued`** |
| `CF-71` | 1 | `23503` `fk_clearance` | `fk_clearance` | **`disposition.fk_clearance`** |

### Removals under which the legal world could not be built

Not admissions, and never counted as depth. A projection that maintains the counter is load-bearing for the LEGAL path as well as the illegal one, so removing it makes the setup itself illegal — the disposition that closes an obligation drives the counter below zero and `ctr_nonneg` refuses it. What this measures is coupling, and it is reported as its own fact rather than folded into a number nobody could interpret.

* `CF-10` — removing `blocking_check@check_materialised`: CF-10: building the LEGAL world failed at 'disposition'. The world a case is illegal in must itself be legal, so this is a broken case or an unmigrated schema, not a refusal. Cause: failed to satisfy 
* `CF-17` — removing `permit_event.linear`: not probed: this mechanism could not be restored after an earlier probe on this cluster, so removing it again would measure a schema short of two mechanisms while reporting one

### Restorations that could not validate

A mechanism whose removal alone admitted the write leaves behind a row it forbids. The tables are append-only, so the row stays and the restoration falls back to `NOT VALID` — the mechanism is re-armed for every subsequent write and the historical row is left where the probe left it, because that row is the evidence. **Every line here is an unwelded gate.**

* `CF-04` — `permit.merge_evidence`
* `CF-05` — `permit.reading_floor_when_issued`
* `CF-07` — `disposition.fk_clearance`
* `CF-13` — `permit_event.legal_edge`
* `CF-18` — `disposition.fk_exposure`
* `CF-23` — `disposition.fk_clearance`
* `CF-31` — `change_request.cr_gate_closed_when_merged`
* `CF-40` — `merge_record.epoch_pin_permit`
* `CF-45` — `permit.gate_closed_when_issued`
* `CF-70` — `permit.gate_closed_when_issued`
* `CF-71` — `disposition.fk_clearance`

### Restorations that failed outright

**Every measurement after one of these is suspect**: the cluster ran the rest of the matrix one mechanism short.

* `CF-14` — permit_event.linear: failed to ingest index entries during backfill: duplicate key value violates unique constraint "linear" DETAIL: Key (permit_id, prev_seq)=('a3b7e649-04a5-5930-8
* `CF-15` — cr_event.cr_linear: failed to ingest index entries during backfill: duplicate key value violates unique constraint "cr_linear" DETAIL: Key (cr_id, prev_seq)=('7ad7252b-004a-534b-8e

## Mechanisms exercised

| mechanism | kind | code | removable |
|---|---|---|---|
| `blocking_check.fk_check_version` | foreign_key | `23503` | yes |
| `blocking_check@check_materialised` | trigger | `P0001` | yes |
| `blocking_check@check_project` | trigger | `P0001` | yes |
| `change_request.cr_gate_closed_when_merged` | check | `23514` | yes |
| `change_request@cr_merge_gate` | trigger | `P0001` | yes |
| `clause_blame_closure@append_only` | trigger | `P0001` | yes |
| `cr_event.cr_linear` | unique | `23505` | yes |
| `disposition.fk_clearance` | foreign_key | `23503` | yes |
| `disposition.fk_exposure` | foreign_key | `23503` | yes |
| `disposition.one_live_disposition` | index | `23505` | yes |
| `disposition@disposition_retract_only` | trigger | `P0001` | yes |
| `merge_record.epoch_pin_permit` | foreign_key | `23503` | yes |
| `merge_record.merge_record_pkey` | unique | `23505` | **no** — a primary key cannot be dropped without replacing it, and CockroachDB retains the old key as a unique index when the primary key is altered — so every available spelling of 'remove this mechanism' either removes two things or removes none, and a probe built on either would report a number that means nothing |
| `permit.gate_closed_when_issued` | check | `23514` | yes |
| `permit.merge_evidence` | check | `23514` | yes |
| `permit.reading_floor_when_issued` | check | `23514` | yes |
| `permit@permit_merge_gate` | trigger | `P0001` | yes |
| `permit_event.legal_edge` | foreign_key | `23503` | yes |
| `permit_event.linear` | unique | `23505` | yes |
| `permit_event@append_only` | trigger | `P0001` | yes |
| `permit_event@permit_event_chain` | trigger | `P0001` | yes |

## The floor

CI fails when a **merge-gate history** — one instantiating `I02` with a declared `refusal_depth_min` of two or more — measures depth below two. The pre-committed response to a depth of one is the kernel lead's, and it is not to relax the floor: *cut the mechanism, do not ship it.* A single-welded gate is a claim that cannot be made under oath.

**Histories below the floor on this run:**

* `CF-01` — depth 1; observed ``.
* `CF-02` — depth 1; observed `mainline.fn_permit_merge_gate`.
* `CF-03` — depth 1; observed `mainline.fn_permit_merge_gate`.
* `CF-04` — depth 1; observed `merge_evidence`.
* `CF-05` — depth 1; observed `reading_floor_when_issued`.
* `CF-06` — depth 1; observed `mainline.fn_permit_merge_gate`.
* `CF-31` — depth 1; observed `cr_gate_closed_when_merged`.
* `CF-45` — depth 1; observed `gate_closed_when_issued`.
* `CF-70` — depth 1; observed `gate_closed_when_issued`.
