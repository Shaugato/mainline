{
  "mcpServers": {
    "crdb": {
      "type": "http",
      "url": "https://cockroachlabs.cloud/mcp",
      "headers": {
        "Authorization": "Bearer ${CC_MCP_API_KEY}",
        "mcp-cluster-id": "${MAINLINE_MCP_CLUSTER_ID}"
      }
    }
  }
}
