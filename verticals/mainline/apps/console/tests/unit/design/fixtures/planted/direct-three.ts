// SPDX-FileCopyrightText: 2026 MAINLINE contributors
// SPDX-License-Identifier: FSL-1.1-ALv2

/**
 * PLANTED VIOLATION — a GPU package in an EVIDENCE directory.
 *
 * `three` is legal in exactly one directory in this console
 * (`src/features/ancestry/render3d/**`). Anywhere else it is the difference between a
 * dimensional surface that can be deleted and one the rest of the console depends on.
 */

import * as THREE from 'three';

export const plantedThree = THREE.MathUtils;
