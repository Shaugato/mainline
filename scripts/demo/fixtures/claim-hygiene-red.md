<!--
SPDX-FileCopyrightText: 2026 MAINLINE contributors
SPDX-License-Identifier: Apache-2.0

⚠ THIS FILE IS DELIBERATELY NON-COMPLIANT AND IS NEVER PUBLISHED. ⚠

It is the RED half of `scripts/demo/claim_hygiene.py`. `.github/workflows/claims.yml` runs

    python scripts/demo/claim_hygiene.py --check scripts/demo/fixtures/claim-hygiene-red.md

and requires it to exit NON-ZERO. A claim-hygiene check that has never failed proves nothing
about a repository whose entire pitch is that it refuses to overclaim (PL-2).

It is NOT in `TARGET_GLOBS`, so the ordinary repository scan never reads it. If you are here
because a scan flagged this file, the bug is in the globs, not in this fixture.

Every sentence below is FALSE about MAINLINE. Do not copy one into anything.
-->

# A page that must not pass

Row-level security in this deployment protects the record even from a rogue admin.

Everything is stored under CMEK with end-to-end Australian data residency.

Our disposition scoring can distinguish a considered disposition from a rubber stamp.

We identity-proof every signer before a credential is enrolled.

`AS OF SYSTEM TIME` lets an auditor read the schema as it stood eighteen months ago.

Every ANN result is bit-identical on replay, and the search is exhaustive over the corpus.

Signing is enclave-attested, and the ledger is split-view resistant today.

Our skill was merged into upstream on the first review.

The gate materialises a blocking check whenever a weakening is proposed.

A disposition of `not_applicable` is available for hazards absent from the site register.

There is judicial precedent for WebAuthn-signed safety records in this jurisdiction.

Detected schema drift triggers auto-remediation on every affected permit.

MAINLINE is an open-source agentic memory layer for industrial operations.

Per-person measurement of deliberation runs across all four sites.

Our self-critical-analysis privilege covers these records.

Privilege extends to operational records held in the same database.

The governing rule here is I07, and the fix landed in commit 7c2e91a.
