# SPDX-FileCopyrightText: 2026 MAINLINE contributors
# SPDX-License-Identifier: FSL-1.1-ALv2
# GOOD. Tree rendering: the vector search feeds a lookup join that fetches the stored vector
# for the exact distance. Extra nodes must not break the assertion.
# Hand-written from the documented fragment; not captured from a cluster.
                                            info
-----------------------------------------------------------------------------------------------
  distribution: local
  vectorized: true

  • render
  │
  └── • lookup join
      │ table: event_cue_embedding@event_cue_embedding_pkey
      │ estimated row count: 12
      │ equality: (cue_id) = (cue_id)
      │ equality cols are key
      │
      └── • vector search
            table: event_cue_embedding@cue_scoped_idx
            target count: 12
            prefix spans: [/'11111111-1111-4111-8111-111111111111'/'6f2c…'/'mechanism' - /'11111111-1111-4111-8111-111111111111'/'6f2c…'/'mechanism']
